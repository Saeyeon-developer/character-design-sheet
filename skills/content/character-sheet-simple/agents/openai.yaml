interface:
  display_name: "Character Sheet Simple"
  short_description: "Generate realistic three-panel character sheets"
  default_prompt: "Use $character-sheet-simple for a realistic person or intentionally minimal character reference with only front, back, and face views; use $character-sheet when side views, expressions, or palettes are required."

policy:
  allow_implicit_invocation: true
